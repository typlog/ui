---
title: Bar Chart
description: Build grouped bar charts with Typlog theme tokens.
status: alpha
---

Pass one y-accessor per series to `VisGroupedBar`. Use the matching `--chart-*` variables for the bars and keep the same key order in `ChartRoot.config` so the legend uses identical colors.

<Example name="charts/Bar.vue" />
